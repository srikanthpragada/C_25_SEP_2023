#include <stdio.h>

#define SIZE 50
// macro
#define iseven(n) n % 2 == 0
#define max(n1, n2)  n1 > n2  ? n1 : n2

void main()
{
  int arr[SIZE];
  int i,a,b;

      for(i = 0; i < SIZE; i ++)
      {
          // process
      }


      for(i = 0; i < SIZE; i ++)
      {
          // process
      }

      if(iseven(i))   //  if (i % 2 == 0)
      {

      }

      printf("%d ", max(a, b));  //  a > b ? a : b


}
